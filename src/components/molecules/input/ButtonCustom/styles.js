import {StyleSheet} from 'react-native';

const styles = StyleSheet.create({
  wrapper: {
    // height: 50,
    marginTop: 13,
    // borderRadius: 20,
    // backgroundColor: 'red',
  },
  text: {
    paddingVertical: 5,
  },
});
export default styles;
