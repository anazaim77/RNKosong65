import {StyleSheet} from 'react-native';

const styles = StyleSheet.create({
  wrapper: {
    height: 50,
    marginTop: 8,
    // borderRadius: 20,
    // backgroundColor: 'red',
  },
});
export default styles;
